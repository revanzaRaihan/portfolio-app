name: 🚀 Deploy to VPS

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  deploy:
    name: Deploy via SSH
    runs-on: ubuntu-latest

    # Hanya deploy saat push ke main, bukan saat PR dibuat
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    steps:
      # ----------------------------------------
      # Step 1: Checkout (untuk validasi file workflow)
      # ----------------------------------------
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      # ----------------------------------------
      # Step 2: Deploy ke VPS via SSH
      # ----------------------------------------
      - name: 🚀 Deploy to VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT }}
          script: |
            set -e  # Stop jika ada error

            echo "📂 Masuk direktori project..."
            cd ${{ secrets.VPS_PATH }}

            echo "🔧 Set git config..."
            git config --global --add safe.directory ${{ secrets.VPS_PATH }}
            git config core.fileMode false

            # ----------------------------------------
            # TULIS .env PERTAMA — sebelum apapun
            # ----------------------------------------
            echo "⚙️ Tulis ulang .env dari secret..."
            cat > .env << 'ENVEOF'
            ${{ secrets.APP_ENV_PRODUCTION }}
            ENVEOF

            echo "⬇️ Pull latest code dari GitHub..."
            git fetch origin
            git reset --hard origin/main

            echo "📦 Install PHP dependencies..."
            COMPOSER_ALLOW_SUPERUSER=1 composer install --no-dev --optimize-autoloader

            echo "🎨 Install Node dependencies..."
            npm ci --legacy-peer-deps

            # ----------------------------------------
            # Generate wayfinder types SEBELUM build
            # ----------------------------------------
            echo "🗺️ Generate wayfinder types..."
            php artisan wayfinder:generate --with-form

            echo "🏗️ Build frontend assets (Vite)..."
            npm run build

            echo "🗄️ Jalankan migration..."
            php artisan migrate --force

            echo "🧹 Clear cache lama..."
            php artisan config:clear
            php artisan route:clear
            php artisan view:clear
            php artisan cache:clear

            echo "⚡ Cache untuk production..."
            php artisan config:cache
            php artisan route:cache
            php artisan view:cache
            php artisan optimize

            echo "🔐 Set permissions..."
            chown -R www-data:www-data storage bootstrap/cache
            chmod -R 775 storage bootstrap/cache

            echo "✅ Deploy selesai!"